// Minimal stub of @minecraft/server for running POC tests in Node.js environment.
// This stub provides no Minecraft functionality — it's only to satisfy imports
// when running unit-style tests that don't interact with the game runtime.

export const world = {
  getEntity: () => undefined,
  getDimension: () => ({ getEntities: () => [], getBlock: () => undefined }),
  scoreboard: {
    getObjective: () => undefined,
    addObjective: () => undefined
  }
};

export const system = {
  run: (fn) => { try { fn(); } catch (e) {} },
  runInterval: (fn, ticks) => { /* noop in tests */ },
  runTimeout: (fn, ticks) => { /* noop in tests */ }
};

export const worldBuilder = {};

export default {};
